# Generated manifest.yml based on template version 0.1.0
# appName = approuter
# language=nodejs
# multitenancy=false
---
applications:

- name: vacation-approuter
  random-route: true  # for development only
  path: .
  memory: 256M
  buildpack: nodejs_buildpack
  services:
  - vacation-xsuaa
  - vacation-html5-runtime

